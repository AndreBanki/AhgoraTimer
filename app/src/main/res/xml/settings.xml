<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <com.banki.main.settings.CustomEditPreference
        android:key="pis"
        android:id="@+id/pis"
        android:title="PIS (apenas dígitos)"
        android:summary="PIS:"
        android:defaultValue=""
        android:selectAllOnFocus="true"
        android:inputType="text"
        android:singleLine="true"
        android:maxLines="1" />

    <com.banki.main.settings.CustomEditPreference
        android:key="empresa"
        android:id="@+id/pis"
        android:title="Empresa"
        android:summary="Código da empresa:"
        android:defaultValue=""
        android:selectAllOnFocus="true"
        android:inputType="text"
        android:singleLine="true"
        android:maxLines="1" />

    <com.banki.main.settings.CustomEditPreference
        android:key="loginTarget"
        android:id="@+id/loginTarget"
        android:title="Login no Target"
        android:summary="Login no TargetProcess:"
        android:defaultValue=""
        android:selectAllOnFocus="true"
        android:inputType="text"
        android:singleLine="true"
        android:maxLines="1" />

    <com.banki.main.settings.CustomEditPreference
        android:key="avisarMinutosAntes"
        android:id="@+id/avisarMinutosAntes"
        android:title="Avisar final do intervalo (minutos)"
        android:summary="Avisar X minutos antes do final do intervalo (em branco desativa):"
        android:defaultValue="5"
        android:selectAllOnFocus="true"
        android:inputType="text"
        android:singleLine="true"
        android:maxLines="1" />

</PreferenceScreen>